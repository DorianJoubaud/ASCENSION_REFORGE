%% ============================================================
%% ASCENSION: Autoencoder-Based Latent Space Class Expansion
%%            for Time Series Data Augmentation
%% ============================================================

\documentclass[10pt]{article}

% Default: anonymous review version. For preprint / camera-ready,
% swap to one of the lines below.
\usepackage{tmlr}
% \usepackage[preprint]{tmlr}   % de-anonymise for arXiv / preprints
% \usepackage[accepted]{tmlr}   % camera-ready (also set \month/\year)
% Compact reference list: natbib hooks (\bibfont is natbib's font hook,
% \bibsep is read at \begin{thebibliography}). tmlr.sty loads natbib.
\renewcommand{\bibfont}{\small}
\setlength{\bibsep}{3pt plus 1pt minus 1pt}

% TMLR's optional notation shortcuts
\input{math_commands.tex}

% ---- Packages ----
\usepackage[hidelinks]{hyperref}
\usepackage{url}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{graphicx}
\graphicspath{{images/}}  % gnuplot \input{} files reference graphics by basename;
                          % without this, \includegraphics{cumulative_sum_*} fails
                          % silently (axis labels render but curve images don't).
\usepackage{wrapfig}    % side-by-side figure beside the Reading paragraph in §3.3
\usepackage{pstricks}   % ICML appendix uses pspicture envs for PSTricks figures
\usepackage{morefloats} % raises the 18-float queue to ~256; detailed classif
                        % appendix has many large tables whose floats would
                        % otherwise overflow at \clearpage ("Float(s) lost")

% Custom commands ported from ICML main.tex preamble
\newcommand{\NA}{--}
\newcommand{\imgsize}{0.15\linewidth}

% Figures directory is named `images/` (matches the `images/...` path prefix
% used in the ICML-ported \includegraphics calls).
\usepackage{subcaption}
\usepackage{amsmath, amssymb, amsthm, mathtools}
% Theorem environments for the formal description in
% Section~\ref{sec:method:alpha} (Definitions 1--2 + Proposition 1).
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\theoremstyle{plain}
\newtheorem{proposition}{Proposition}
% Gray box around Definition and Proposition environments (visual
% emphasis; TMLR has no rule against this --- mdframed is widely
% used in accepted papers).
\usepackage{mdframed}
\mdfdefinestyle{definitionbox}{%
  backgroundcolor=black!5,
  linewidth=0pt,
  innertopmargin=3pt,
  innerbottommargin=3pt,
  innerleftmargin=10pt,
  innerrightmargin=10pt,
  skipabove=5pt,
  skipbelow=5pt,
}
\surroundwithmdframed[style=definitionbox]{definition}
\surroundwithmdframed[style=definitionbox]{proposition}
% `lscape` is already pulled in by `pdflscape` above; loading it a second
% time here caused \begin{landscape} to be redefined to the non-rotating
% variant, breaking float placement and triggering "Float(s) lost" at
% \clearpage. Keep only pdflscape.
\usepackage{svg}
\usepackage{bm}
\usepackage{float}          % enables [H] float placement
\usepackage{algorithm}
\usepackage{placeins}
\usepackage{algpseudocode}
\usepackage{xcolor}
% todonotes warns if marginparwidth < 2cm — set it before loading.
% `textsize` must be a package load-time option; the remaining knobs
% are configured via \setuptodonotes{} once the package is loaded.
\setlength{\marginparwidth}{2cm}
\usepackage[textsize=footnotesize]{todonotes}
\setuptodonotes{linecolor=orange!50,backgroundcolor=orange!10,bordercolor=orange}

% ---- Drafting shortcuts ----
\newcommand{\fix}{\marginpar{FIX}}
\newcommand{\new}{\marginpar{NEW}}
\newcommand{\rev}[2]{\textit{[R\##1:}~#2\textit{]}}     % mark review-driven additions inline

% ---- Title & metadata ----
\title{ASCENSION: Autoencoder-Based Latent Space Class Expansion for Time Series Data Augmentation}

% Camera-ready author block. Addresses and affiliations confirmed by
% Dorian on 3 sept 2026.
\author{\name Dorian Joubaud \email dorian.joubaud@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg \AND
      \name Matthieu Olekhnovitch \email matthieu.olekhnovitch@polytechnique.edu \\
      \addr \'Ecole Polytechnique, Palaiseau, France \AND
      \name Adrien Bolling \email adrien.bolling@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg \AND
      \name Evgeny Zotov \email evgeny.zotov@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg \AND
      \name Sylvain Kubler \email sylvain.kubler@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg \AND
      \name Maxime Cordy \email maxime.cordy@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg \AND
      \name Mike Papadakis \email mike.papadakis@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg \AND
      \name Yves Le Traon \email yves.letraon@uni.lu \\
      \addr University of Luxembourg, SnT, Luxembourg}

% STILL OPEN: the publication month, set to 09/2026 as a placeholder,
% and the OpenReview forum id. The month and year print in the running
% header of every page, the forum line under the author block, so
% neither can stay as it is once [accepted] is switched on.
\def\month{09}
\def\year{2026}
\def\openreview{\url{https://openreview.net/forum?id=XXXX}}

\begin{document}
\maketitle

\begin{abstract}
Achieving effective data augmentation (DA) in time series (TS)
classification is challenging due to the complex nature of
temporal data. While state-of-the-art generative models for DA,
based on generative adversarial networks, diffusion models and variational autoencoders (VAEs), demonstrate potential,
they often fail to yield consistent performance gains across
diverse domains (e.g., ECG, power, vibration). To overcome this,
we propose \textbf{ASCENSION} (\textbf{A}utoencoder-based latent
\textbf{S}pace \textbf{C}lass \textbf{ExpaNSION}), a novel
generative framework that leverages the probabilistic nature of a
VAE latent space together with a contrastive loss to promote
intra-class compactness and inter-class separability. Its key
innovation is an $\alpha$-scaling mechanism that progressively
expands per-class posterior covariances while preserving class
identity, populating under-represented neighbourhoods beyond the
training distribution. We evaluate ASCENSION on 102 univariate datasets
from the UCR benchmark using two established deep TS
classifiers and a recent TS foundation model, comparing
it against eight state-of-the-art DA methods. Empirical results
% REVISED FOR TMLR (rebuttal C2/C3)
demonstrate that, under a single fixed configuration and
averaged over three runs, ASCENSION increases mean
classification accuracy by $+1.3\%$ and $+0.6\%$ on the two deep
classifiers and by $+0.8\%$ on the foundation model, while the
strongest baseline, VaDE, results
in a $-0.9\%$ decrease averaged across the three classifiers. Notably, ASCENSION delivers non-negative
gains on $71.9\%$, $62.7\%$ and $62.4\%$ of the datasets,
respectively. An ablation study
further highlights the significant impact of the $\alpha$-scaling
mechanism on these gains, and the benefit concentrates where
training data is scarce: the datasets with the fewest training
samples per class benefit most. These findings
position ASCENSION as a low-risk DA method for the
small-training-set regime. It is the only method in our benchmark
with a positive mean gain on each of the three classifiers, and it
is never significantly worse than any competitor, without
requiring prior knowledge of
method suitability.
\end{abstract}

% ==========================================================
% 1. Introduction
% ==========================================================
\input{sections/01_introduction}

% ==========================================================
% 2. Problem Setup (folds Related Work — TMLR DA paper convention)
% ==========================================================
\input{sections/02_problem_setup}

% ==========================================================
% 3. Proposed Method (with new 3.1 Motivation subsection)
% ==========================================================
\input{sections/03_method}

% ==========================================================
% 4. Experiments (setup + results + analysis merged)
% ==========================================================
\input{sections/04_experiments}

% ==========================================================
% 5. Conclusion
% ==========================================================
\input{sections/05_conclusion}

% ==========================================================
% 6. Limitations and Future Work
% ==========================================================
\input{sections/06_limitations}

% ==========================================================
% Broader Impact Statement (TMLR-required)
% ==========================================================
\input{sections/07_broader_impact}

% ==========================================================
% Reproducibility Statement + Acknowledgements
% ==========================================================
\input{sections/08_reproducibility}

\bibliography{references}
\bibliographystyle{tmlr}
\newpage
\appendix
\input{sections/appendix}

\end{document}
